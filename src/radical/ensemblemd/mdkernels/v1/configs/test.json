{
    "kernel_name": "TEST",
    "kernel_configs": {

        "localhost": {
            "environment"   : {},
            "pre_exec"      : ["/bin/echo -n TEST:localhost"],
            "executable"    : "/bin/hostname"
        },
        "test.host.02": {
            "environment"   : {},
            "pre_exec"      : ["/bin/echo -n TEST:test.host.02"],
            "executable"    : "/bin/hostname"
        }
    }
}
